-- V4: Password reset columns are already included in create_tenant_schema() function in V3.
-- This migration is intentionally left empty.
-- Existing tenant schemas need manual column addition via the tenant schema update process.
SELECT 1;